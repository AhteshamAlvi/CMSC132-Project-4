package game;

import java.util.ArrayList;
import java.util.Random;

import java.awt.*;
import java.awt.event.*;


import java.awt.Canvas;
import java.awt.Graphics;
import java.awt.Point;

public class Map extends Canvas {
	
	Point[] squarePoints;
	Point b;
	Square square1, square2;
    private Square square;

    public Map() {
        
    	super();
		this.setFocusable(true);
		this.requestFocus();
		
		squarePoints = new Point[4];
		squarePoints[0] = new Point(0,0);
		squarePoints[1] = new Point(30,0);
		squarePoints[2] = new Point(30,30);
		squarePoints[3] = new Point(0,30);
		b = new Point(400,300);
		
		this.square1 = new Square(squarePoints, b, 0);
		
    }

    @Override
    public void paint(Graphics brush) {
        // Draw the square
    	  Square random = new Square(getWidth(), getHeight());
    }

    
}
